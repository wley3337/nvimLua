return {
  settings = {
    python = {
      analysis = {
        -- typeCheckingMode = "off", --  opts: "off", "basic" default, "strict"
        -- python.venvPath: "", -- path to virtual environments
      },
    },
  },
}
